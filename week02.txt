week02
- linux commands
	ls, ls -a, ls -l, ls -al
	pwd, cd .git, cd .., touch week01.txt
	vi week01.txt, vi week02.txt
	echo "sldkfsldfkjlskdfj" >> README.md
	cat README.md

- git commands
	git init
	git status
	git add week01.txt
	git config --global user.email "asdf@example.com"
	git config --global user.name "hjkl"
	git commit -m "v0"
	git log
	git branch -M main
	git remote add origin https://github.com/chanjung0916/test.git
	git push -u origin main
